import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF2EA063); // AzorTech Green
  static const background = Color(0xFF111827); // Dark BG
  static const surface = Color(0xFF1F2937); // Card BG
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.grey;
  
  // Cores de Status
  static const statusDone = Color(0xFF10B981);
  static const statusPending = Color(0xFFF59E0B);
  static const statusLate = Color(0xFFEF4444);
}
